import './globals.css'

export const metadata = {
  title: 'WorthIt — What could your tech sell for?',
  description: 'Estimate UK resale value for used electronics.',
}

export default function RootLayout({ children }) {
  return <html lang="en-GB"><body>{children}</body></html>
}
